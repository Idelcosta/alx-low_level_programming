#include <stdio.h>

/**
 * main - Entry point
 * define variable
 * also an iterrative variable
 * Return: Always 0 (Succes)
 */
int main(void)
{
	char A;
	for (A = 'a'; A <= 'z'; A++)
	{
		putchar(A);
	}

	putchar('\n');

	return (0);
}
