#include <stdio.h>

/**
 * main - Entry point
 * define variable
 * also an iterrative variable
 * Return: Always 0 (Succes)
 */
int main(void)
{
	char a;

	for (a=65; a<=90; ++a)
	{
		putchar(a);
	}
	return (0);
}
